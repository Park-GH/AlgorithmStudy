#include <iostream>
using namespace std;

int main() {
	// . = 0 S = -1 D = -2 * = -3 X = -4 
	int map[52][52] = { 0, };
	bool marked[52][52] = { 0, };
	int r, c;
	cin >> r >> c;
	char ch;
	cin >> r >> ch;
	for(int i=0; i < r; i++)
		for (int j = 0; j < c; j++)
		{
		}

}